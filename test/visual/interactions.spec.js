const{expect:expect,test:test}=require("@playwright/test");test("navigation exposes only CV-backed site sections",async({page:t})=>{await t.goto("/",{waitUntil:"networkidle"});const e=await t.locator(".nav-menu .nav-link").allTextContents();expect(e.map(t=>t.trim())).toEqual(["Publications","Talks","Teaching"]),await expect(t.getByRole("link",{name:"blog",exact:!0})).toHaveCount(0),await expect(t.getByRole("link",{name:"repositories",exact:!0})).toHaveCount(0),await expect(t.getByRole("link",{name:"CV",exact:!0})).toHaveCount(0)}),test("home has one obfuscated, non-clickable email, one CV link, and no profile image",async({page:t})=>{await t.goto("/",{waitUntil:"networkidle"}),await expect(t.locator('a[href^="mailto:"]')).toHaveCount(0),await expect(t.locator("#contact-email")).toHaveText("jaeyoungkim22 at snu.ac.kr"),await expect(t.locator("#contact-email")).toHaveJSProperty("tagName","SPAN"),await expect(t.locator('a[href$="/assets/pdf/CV_Jaeyoung_Kim.pdf"]')).toHaveCount(1),await expect(t.locator("main img")).toHaveCount(0),await expect(t.getByText("Selected publications",{exact:!1})).toHaveCount(0)}),test("publication abstract expands without exposing BibTeX",async({page:t})=>{await t.goto("/publications/",{waitUntil:"networkidle"});const e=t.locator(".publication-abstract").first();await expect(e).not.toHaveAttribute("open",""),await e.locator("summary").click(),await expect(e).toHaveAttribute("open",""),await expect(e.locator("p")).toContainText("quantum channels of rank at most three"),await expect(t.locator('a[href^="https://doi.org/"]')).toHaveCount(1),await expect(t.locator('a[href^="https://arxiv.org/"]')).toHaveCount(1),await expect(t.getByText("BibTeX",{exact:!1})).toHaveCount(0)}),test("talk titles render inline mathematics without raw Markdown delimiters",async({page:t})=>{await t.goto("/talks/",{waitUntil:"networkidle"}),await expect.poll(async()=>t.locator("mjx-container").count()).toBe(7),await expect(t.locator("main")).not.toContainText("$")}),test("teaching contains only entries synchronized from the CV",async({page:t})=>{await t.goto("/teaching/",{waitUntil:"networkidle"}),await expect(t.locator(".teaching-list li")).toHaveCount(8),await expect(t.getByText("Differential and Integral Calculus Practice",{exact:!1})).toBeVisible(),await expect(t.getByText("Teaching Volunteer",{exact:!1})).toBeVisible(),await expect(t.getByText("Machine Learning",{exact:!1})).toHaveCount(0),await expect(t.getByText("Data Science",{exact:!1})).toHaveCount(0)});